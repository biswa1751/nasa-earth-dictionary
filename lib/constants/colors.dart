import 'package:flutter/material.dart';

class AppColors {
  // NASA-inspired color palette
  static const primaryBlue = Color(0xFF0066CC);
  static const secondaryBlue = Color(0xFF4A90E2);
  static const accentOrange = Color(0xFFFF6B35);
  static const darkBlue = Color(0xFF003366);
  static const lightBlue = Color(0xFF7DD4F7);

  // Legacy color for backward compatibility
  static const color = lightBlue;

  // Additional theme colors
  static const surfaceBlue = Color(0xFFF0F8FF);
  static const onSurfaceBlue = Color(0xFF1A365D);
  static const errorRed = Color(0xFFE53E3E);
  static const successGreen = Color(0xFF38A169);
}
